# EmailJS設定（開発環境/GitHub Pages用）
# EmailJSのダッシュボードから取得してください
# https://dashboard.emailjs.com/

VITE_EMAILJS_PUBLIC_KEY=your_public_key_here
VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here

# PHP/SMTP切り替え設定
# ロリポップサーバーで動作させる場合は true に設定
VITE_USE_PHP=false

# 使い方:
# 1. EmailJSでアカウントを作成
# 2. Email Serviceを追加（Gmail等）
# 3. Email Templateを作成
# 4. 上記の値をコピーして.envファイルに設定
# 5. .envファイルをgitignoreに追加（既に追加済み）
#
# ロリポップサーバーで使用する場合:
# - VITE_USE_PHP=true に設定
# - private/config.php のSMTP設定を確認